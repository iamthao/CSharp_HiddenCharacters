SELECT * FROM day
WHERE date(firstdayofweek) >=
(SELECT CURDATE() - INTERVAL (WEEKDAY(CURDATE())+1) DAY) 
AND date(lastdayofweek) <=
(SELECT DATE_ADD((SELECT CURDATE() - INTERVAL (WEEKDAY(CURDATE())+2)DAY),INTERVAL 7 DAY))